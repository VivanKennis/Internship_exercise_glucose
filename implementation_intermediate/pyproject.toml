[project]
name = "systems-biology-project-template-slimmed"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib",
    "numpy",
    "pandas",
    "python-dateutil",
    "scipy",
    "sund",
]
